machine:
  node:
    version: 6.10.1
  java:
    version: oraclejdk8


deployment:
  production:
    branch: deployment
    commands:
      - cd client
      - sudo npm install -g cordova ionic
      - npm install
      - ionic state restore
      - ionic serve
