machine:
  node:
    version: 6.10.1
  java:
    version: oraclejdk8


deployment:
  production:
    branch: deployment
    commands:
      - npm install -g cordova ionic
      - cd client && npm install
      - ionic state restore
      - ionic serve
